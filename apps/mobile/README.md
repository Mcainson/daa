# DAA Mobile

The iOS-first React Native client for Daily Accountability Agent.

## Commands

```powershell
pnpm dev
pnpm dev:web
pnpm check
```

The app uses Expo Router and a development-build-compatible architecture. The
current vertical slice keeps state locally in memory while the SQLite and API
adapters are built behind the shared domain contracts.
